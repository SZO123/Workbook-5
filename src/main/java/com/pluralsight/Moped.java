package com.pluralsight;

public class Moped extends Vehicles {

public Moped(String make, String model, int year, double maxSpeed, boolean isElectric) {
    super(make, model, year, maxSpeed, isElectric);
}
}