package com.pluralsight;

public class Car extends Vehicles {
    public Car(String make, String model, int year, double maxSpeed, boolean isElectric) {
        super(make, model, year, maxSpeed, isElectric);
    }
}