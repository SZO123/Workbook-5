package com.pluralsight;

public class SemiTruck extends Vehicles {
    public SemiTruck(String make, String model, int year, double maxSpeed, boolean isElectric) {
        super(make, model, year, maxSpeed, isElectric);
    }
}