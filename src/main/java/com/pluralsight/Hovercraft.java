package com.pluralsight;

public class Hovercraft extends Vehicles {
    public Hovercraft(String make, String model, int year, double maxSpeed, boolean isElectric) {
        super(make, model, year, maxSpeed, isElectric);
    }
}
